import { PageHeader } from "../../components/PageHeader";

// Conteúdo real: PRD 08 (frontend-prds/08-tela-painel.md). Renderizada
// dentro do <Outlet /> de RepoAreaLayout na rota índice "/repos/:id".
export function DashboardPage() {
  return <PageHeader title="Painel" description="Overview — PRD 08" />;
}
