import { PageHeader } from "../../components/PageHeader";

// Conteúdo real: PRD 03 (frontend-prds/03-tela-landing.md). Este placeholder
// só prova que a rota "/" existe e renderiza dentro do PublicLayout.
export function LandingPage() {
  return (
    <div className="mx-auto max-w-3xl p-8">
      <PageHeader level="h1" title="Bella Reviewer" description="Landing page — PRD 03" />
    </div>
  );
}
